#ifndef PRISMA_SHADER
#define PRISMA_SHADER

namespace prisma {

class Shader {
 public:
  void AddMatrix(const std::string& name);
  void AddVector(const std::string& name);
 private:
};
  
}  // namespace prisma

#endif
